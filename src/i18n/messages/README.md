# Locale dosyaları

Her dil için `{kod}.json` dosyası ekleyin (ör. `de.json`).

Ardından `src/i18n/config.ts` içinde:

- `locales` dizisine kodu ekleyin (`"de"`)
- `localeNames` içine görünen adı ekleyin (`de: "Deutsch"`)

`tr.json` dosyası şablon olarak kullanılabilir.
